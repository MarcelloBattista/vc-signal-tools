#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift(File.join(__dir__, "..", "lib"))

require "dotenv/load"
require "insight_extraction_service"

VCTools::Podcast::InsightExtractionService.new.run(limit: ENV["LIMIT"]&.to_i)

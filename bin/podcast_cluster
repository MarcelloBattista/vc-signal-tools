#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift(File.join(__dir__, "..", "lib"))

require "dotenv/load"
require "clustering_service"

VCTools::Podcast::ClusteringService.new.run(limit: ENV["LIMIT"]&.to_i)

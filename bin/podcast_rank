#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift(File.join(__dir__, "..", "lib"))

require "dotenv/load"
require "ranking_service"

VCTools::Podcast::RankingService.new.run(limit: ENV["LIMIT"]&.to_i)
